package leetcode.easy;

public class ReserveBits {

public int reverseBits(int n) {
     StringBuffer reResult = new StringBuffer();
     reResult.append(Integer.toBinaryString(n));
     reResult.reverse();
     //byte result = (byte)reResult.toString();
     //byte[] result = new byte[reResult.length()];
     //result = reResult.toString();
    return n;
     
             
             
     
     //Byte.toUnsignedInt(result);
     
    }
}
